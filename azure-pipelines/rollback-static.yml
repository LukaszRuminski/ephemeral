trigger:
  - none

variables:
  - group: 'project variables - prod'
  - group: 'hosted-login.aceteam.tech - application - prod'

resources:
  repositories:
    - repository: templates
      type: git
      name: MW-Framework/MW-azure-pipelines

extends:
  template: rollback-static.yml@templates
